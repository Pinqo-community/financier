version: "3"

services:
    php:
        image: php:8.2-fpm-alpine
        build:
            context: ./docker/php
        volumes:
            - ./:/var/www/html
        networks:
            - symfony_app

    nginx:
        image: nginx:latest
        ports:
            - "80:80"
        volumes:
            - ./:/var/www/html
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
        networks:
            - symfony_app
    db:
        image: postgres
        volumes:
            - "db_app:/var/lib/postgres"
        environment:
            POSTGRES_DB: ${PG_DATABASE}
            POSTGRES_USER: ${PG_USER}
            POSTGRES_PASSWORD: ${PG_PASSWORD}
        ports:
            - "5432:5432"
        networks:
            - symfony_app

volumes:
    db_app:

networks:
    symfony_app:
